import Pet from '../src/petObject.js';

describe('animal', function() {
  it ('should test whether hungerLevel increases by one every 5 seconds', function() {
    expect(pet1.hungerLevel).toEqual(1);
  })
})
